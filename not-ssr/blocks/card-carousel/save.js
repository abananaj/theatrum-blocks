/**
 * Card Carousel Block - Save
 * 
 * This block uses server-side rendering (render.php), so the save function returns null.
 * The block content is rendered on the server using the render_callback.
 */

export default function Save() {
	return null;
}
