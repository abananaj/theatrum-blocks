# Meta Date Block

Display date values from post metadata with flexible formatting and parsing. Automatically converts date meta fields to formatted date strings.

## Key Features

- Intelligent date format parsing (supports multiple input formats)
- Configurable output date format
- Flexible HTML tag selection for rendering
- Full typography and color customization controls
- Handles edge cases and validates date ranges
