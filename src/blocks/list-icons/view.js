/**
 * Icon List Block - Frontend
 * Handles any client-side interactions needed for icon list display
 */

document.addEventListener( 'DOMContentLoaded', function () {
	// Icon list is mostly static, but this file is needed for wp-scripts to compile the block
	// Any client-side interactivity can be added here
} );
