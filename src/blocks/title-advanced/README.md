REBUILD ⚠️
Turn into a Heading Group block that holds two heading blocks, where the second is immediately below the first and styled as a subtitle. The first heading block should be styled as a title, and the second heading block should be styled as a subtitle. The title and subtitle should be able to be edited independently, and the subtitle should have a smaller font size than the title. The block should have a class of "title-subtitle" for styling purposes. By default the second heading should be set to the next heading level down, but the user can overrise this. The block should be responsive and look good on all screen sizes. The title and subtitle should be able to be aligned independently, and the block should have options for spacing and color. The block should also have a option to add a background color or image. The block should be easy to use and customize, and should fit seamlessly into any design.
